import 'package:flutter/material.dart';

class ListeAgents extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Expanded(
          flex: 3,
          child: Container(),
        ),
        Expanded(
          flex: 6,
          child: Container(),
        )
      ],
    );
  }
}
